<!DOCTYPE html>
<html lang="{{ meta.lang }}" data-user-color-scheme="light" class="{{ theme }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ metaTitle or meta.title }}</title>

  <link rel="stylesheet" href="{{ '/assets/styles/screen.css' | url }}">
  {% include "meta.njk" %}

  <!-- Fathom - beautiful, simple website analytics -->
  <script src="https://cdn.usefathom.com/script.js" data-site="FUHXOCJU" defer></script>
  <!-- / Fathom -->
</head>
<body>
  <div hidden>{% iconsprite %}</div>
  <a href="#main" class="c-skiplink">Skip to main content</a>

  <div role="document">
    {% include "header.njk" %}
    <main id="main" class="o-layout u-focus-hidden" tabindex="-1">
      {{ content | safe }}
    </main>
    {% include "footer.njk" %}
  </div>

  <script type="text/javascript" src="{{ '/assets/scripts/main.js' | url }}"></script>
  <!--! LAST BUILD: {{ build.env}} {{ build.timestamp | dateToISO }} -->
</body>
</html>
